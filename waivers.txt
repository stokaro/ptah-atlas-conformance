# Conformance waivers.
#
# The conformance gate is RED until Ptah covers everything Atlas expresses in the
# vendored corpus. A gap/fail/panic fails the gate unless it is waived here.
#
# Format, one per line:
#   <probe> <fixture> <stage>   <reason> (ptah#NNN)
#
# The first three space-separated tokens are the match key (probe, fixture,
# stage); everything after is the human reason and must cite the tracking issue
# or say why the divergence is intentional and permanent.
#
# A waiver only means "this gap is consciously tracked, do not fail the build on
# it yet" — NOT "this is fine forever". Keep this list as short as honesty allows.
# An empty list means: nothing is intentionally skipped, so every open gap is red.
#
# Everything not listed below is a real, un-excused distance from Atlas, and the
# gate stays red until it closes.

txtar-script internal/integration/testdata/postgres/column-enum-array.txtar script-runtime   The fixture expects an enum array column to inspect back as sql("status[]"); Ptah writes it schema-qualified. Intentional and permanent rather than tracked-to-close: the bare spelling resolves through search_path at replay, so a plan built from it fails with `type "mood[]" does not exist`. Measured on PostgreSQL 17.10, where qualifying the array spelling moved a replayed schema from exit 3 to exit 0 (ptah#1138).
