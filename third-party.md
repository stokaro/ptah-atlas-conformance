# Ptah third-party Atlas repository conformance report

This file is generated by `make probe-third-party`. Do not edit by hand.

It records whether real repositories that use Atlas keep working when the
Atlas binary is replaced by `ptah-compat`, with nothing else changed. Each
row is a command run on both binaries in independent copies of the same
pinned tree, compared on exit code and on the bytes the command left behind.
It is a drop-in replacement probe, not a coverage score for those projects.

## Status: PARITY on the current corpus

Every fixture is covered. The conformance gate is green.

- Upstream repositories pinned by commit in `third-party-repos.json`, fetched with git so the object hashes are verified; measured against `atlas community version v1.3.0`
- Ptah at `ptah.run v0.4.1-0.20260907150813-efe7559b7282`
- Outcomes: **3 ok**, **0 gap**, **0 fail**, **0 panic**
- Full gate: **0 non-OK** (passes CI)
- Regression budget input: **0 unwaived non-OK**, 0 waived

## Compared Schema Fact Categories

- Checksum parity: re-hashing a migration directory its own author hashed must leave `atlas.sum` byte-identical, on both binaries.
- Checksum-only validation: `migrate validate` without `--dev-url` replays nothing and needs no database, and must agree on exit code.
- Configuration formatting: formatting the project's committed `atlas.hcl` must rewrite nothing.

## Findings

| Gate | Outcome | Probe | Fixture | Stage | Detail | Related |
| --- | --- | --- | --- | --- | --- | --- |
| — | ok | third-party | `go-faster/bot` | migrate hash | exit 0 on both binaries; migrations/atlas.sum byte-identical to the upstream commit |  |
| — | ok | third-party | `go-faster/bot` | migrate validate | exit 0 on both binaries; migrations/atlas.sum byte-identical to the upstream commit |  |
| — | ok | third-party | `go-faster/bot` | schema fmt | exit 0 on both binaries; atlas.hcl byte-identical to the upstream commit |  |
