# Maximum allowed unwaived non-OK observations in docs-surface.json.
# Ratchet this down as atlasgo.io docs pages get triaged in
# docs-surface-registry.json; CI fails on regressions above it.
# Seed state: 322 of 351 pages are untriaged (29 are triaged as cloud).
#
# 2026-09-07: 322 -> 338. Upstream published sixteen pages -- five dbt guides,
# two Redshift security guides, two YugabyteDB guides, two competitor
# comparisons, a ClickHouse WASM guide, a database-per-tenant guide, a security
# scanning guide, a Kubernetes versioned quickstart and a data-scripts page --
# and each joined the backlog its own family already sits in: every existing
# /guides/redshift, /guides/yugabyte, /guides/clickhouse and
# /integrations/kubernetes page is untriaged too. Triaging the new ones while
# their siblings are undecided would record a scope verdict nobody took, and an
# out-of-scope on a page Ptah should cover hides the gap instead of naming it.
#
# The ceiling therefore tracks what upstream publishes, and the ratchet applies
# to the backlog: this number goes down when pages get triaged, never when Atlas
# writes fewer docs.
# 2026-09-07: 338 -> 318. First triage batch. Every verdict is transcribed
# from ptah's own docs/site/src/content/docs/atlas/docs-coverage.md coverage
# matrix rather than decided here: Documented and Native become `open`, Partial
# becomes `partial`. Twenty pages had an area in that matrix; nothing else did,
# and inventing the rest is what this backlog exists to prevent.
# 2026-09-12: 318 -> 271. Second batch, and every verdict follows from
# something already decided rather than from a judgment taken here. Twelve
# /guides pages name an engine core/platform.NormalizeDialect does not accept
# -- redshift, snowflake, dsql, databricks, horizondb, azure-fabric -- so the
# page documents a target Ptah does not have, and adding one is a product step
# rather than a docs gap. Thirty-five /guides/orms pages are onboarding or
# landing text for running Atlas beside a third-party ORM, which Ptah does not
# publish: it declares a schema from Go annotations.
#
# The twenty-one /guides/orms pages that name a capability instead -- RLS,
# composite, domain and enum types, triggers, views, linting -- are deliberately
# left untriaged. They are about what Ptah models, not about the ORM in their
# path, and an out-of-scope there would hide the gap this backlog exists to
# name.
# 2026-09-12: 271 -> 200. Seventy-one guide pages triaged in one pass. Every
# verdict is grounded in a measurement rather than in a reading of the page: a
# `ptah` or `ptah-compat` invocation with its output quoted, or the declaration
# that parses, renders and compares the construct.
#
# Ten pages name something Ptah does not model at all and are recorded as gap:
# unlogged tables, ALTER DEFAULT PRIVILEGES, Oracle VECTOR, Spanner protobuf
# columns, ClickHouse named collections and WASM functions, YugabyteDB
# colocation and pre-splitting, PostgreSQL template databases for schema tests,
# and a Terraform provider. Forty-four are partial and seventeen open.
#
# Eleven more pages carry a proposed verdict that the verification pass did not
# confirm, and they stay untriaged. An unconfirmed verdict in this file is worth
# less than the absence it would replace. Seven of them are /guides/orms
# capability pages, which the entry above already held back for the same reason.
200
